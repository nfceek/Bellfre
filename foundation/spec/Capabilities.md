
because the manifest depends on a defined capability vocabulary.

The key design decision we are establishing here:

**The manifest describes capability. It does not describe implementation.**

That separation is what allows Bellfre to support:
- a personal AI agent,
- a small business service,
- an enterprise platform,

without creating separate ecosystems.